extends layout

block content
  h1= title

  ul
    each creator in animeCreator_list
      li
        a(href=creator.url) #{creator.name}
        |  (#{creator.date_of_birth_formatted} - #{creator.date_of_death_formatted})

    else
      li There are no authors.
